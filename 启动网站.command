#!/bin/bash
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )"
cd "$DIR"
echo "============================================================"
echo "🚀 正在启动【CRS 涉税金融资产与证券交易盈亏智能核算系统】网站..."
echo "============================================================"
python3 "$DIR/start_app.py"
